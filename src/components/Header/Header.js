import React from 'react';

import './Header.css';

const Header = () => (
    <div className="header">
        <div className="header__container">
            <nav>
                <span className="header__span">Note Management App</span>
            </nav>
        </div>
    </div>
);

export default Header